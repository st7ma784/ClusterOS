# ClusterOS Image Build Secrets
# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed

# =============================================================================
# TAILSCALE CONFIGURATION (Required for multi-node clusters)
# =============================================================================

# Tailscale OAuth Client (RECOMMENDED - keys never expire)
# Get from: https://login.tailscale.com/admin/settings/oauth
# Required scopes: Devices (create)
TAILSCALE_OAUTH_CLIENT_ID=
TAILSCALE_OAUTH_CLIENT_SECRET=

# Tailscale Auth Key (FALLBACK - expires after 90 days max)
# Get from: https://login.tailscale.com/admin/settings/keys
# Only used if OAuth fails
TAILSCALE_AUTHKEY=

# =============================================================================
# CLUSTER AUTHENTICATION KEY (Required)
# =============================================================================
# This key ensures only nodes from this build can join each other.
# If empty, it will be auto-generated during build and saved to cluster.key
#
# To use an existing key: CLUSTER_AUTH_KEY=$(cat /path/to/cluster.key)
# To generate manually: openssl rand -base64 32
CLUSTER_AUTH_KEY=

# Serf encryption key (optional, for additional security)
# If empty, same as CLUSTER_AUTH_KEY will be used
SERF_ENCRYPT_KEY=

# =============================================================================
# WIFI CONFIGURATION (Optional)
# =============================================================================
WIFI_SSID=
WIFI_KEY=
