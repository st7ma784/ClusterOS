# Tailscale Configuration for ClusterOS
# This file is installed during image build
#
# OAuth Client credentials (RECOMMENDED - never expire)
# Get yours from: https://login.tailscale.com/admin/settings/oauth
# Required scopes: devices (create)
#
# IMPORTANT: Replace these with your own credentials!
# These example keys will NOT work.

TAILSCALE_OAUTH_CLIENT_ID=
TAILSCALE_OAUTH_CLIENT_SECRET=

# Tags to apply to cluster nodes (for Tailscale ACLs)
# Format: ["tag:cluster-node", "tag:other"]
TAILSCALE_TAGS=["tag:cluster-node"]

# Optional: Override hostname (defaults to system hostname)
# TAILSCALE_HOSTNAME=
